# Release Notes

Each Modrinth upload must use a concise per-version notes file:

```text
gradle/release-notes/<mod_version>.md
```

Keep user-visible changes here. Internal scaffolding, Gradle plumbing, and
research history belong in `CHANGELOG.md` and `TODO.md`.
