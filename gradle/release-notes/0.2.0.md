Quick Stack Nearby 0.2.0 work in progress.

- Replaces the inventory button glyph with a clearer chest-and-arrow icon.
- Keeps the quick-stack button aligned when the recipe book shifts the
  inventory screen.
- Adds op-only commands to inspect, update, and reload the nearby-container
  scan range. The range is saved in the shared TempestStudios app-data folder.
- Adds a right-click slot rules screen for the quick-stack button. Main
  inventory slots can be locked from unloading or assigned a keep count so a
  chosen number of items stays in the player inventory.
